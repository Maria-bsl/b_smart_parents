import { IsVisibleNavbarPipe } from './is-visible-navbar.pipe';

describe('IsVisibleNavbarPipe', () => {
  it('create an instance', () => {
    const pipe = new IsVisibleNavbarPipe();
    expect(pipe).toBeTruthy();
  });
});
